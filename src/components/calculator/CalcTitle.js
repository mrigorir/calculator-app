// imports
import React from 'react';

// Components
const CalcTitle = () => (
  <header>
    <h1 className="calcTitle">
      Let&apos;s do some Math!
    </h1>
  </header>
);

export default CalcTitle;
